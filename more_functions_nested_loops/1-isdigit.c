/**
 * _isdigit - Check is between 0-9
 * with return
 * Return: 1 for true et 0 for falsE
 * @c : Input to check
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
