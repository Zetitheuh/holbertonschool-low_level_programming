#include <stdio.h>

/**
 * main - Entry of the program
 * this functions print a message to the console using printf
 *
 * Return: Zero = The program was successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
